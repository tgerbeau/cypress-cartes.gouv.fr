/**
 * test-email.cy.js
 * Test temporaire pour vérifier l'envoi d'email en CI
 * SUPPRIMER APRÈS VÉRIFICATION
 */
describe('Test notification email', () => {
  it('ce test échoue volontairement pour déclencher l\'email', () => {
    expect(true).to.eq(false)
  })
})
